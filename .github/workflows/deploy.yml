name: Deploy to Azure

on:
  push:
    branches: [main]
  workflow_dispatch:

# id-token: write ermöglicht den OIDC-Login gegen Azure (azure/login tauscht das
# GitHub-OIDC-Token gegen ein kurzlebiges Azure-Access-Token, ohne dass irgendwo ein
# langlebiger Client-Secret-Key als GitHub-Secret liegen muss).
# packages: write wird für den Push des Backend-Images nach GHCR gebraucht.
# Nie zwei Deploys gleichzeitig: Azure lehnt parallele Infrastruktur-Deploys
# ab (DeploymentActive). Ein zweiter Lauf wartet, statt abzubrechen; wartet
# schon einer, ersetzt ihn der neueste (der enthält ohnehin alle Commits).
concurrency:
  group: deploy-production
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read
  packages: write

env:
  AZURE_RESOURCE_GROUP: trip-planner-dev-rg
  AZURE_LOCATION: germanywestcentral
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/ai-trip-planner-api
  RAG_IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/ai-trip-planner-rag

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # --- 1. Backend-Image bauen und nach GHCR pushen -------------------------------
      # Muss VOR dem Bicep-Deployment passieren: die Container App referenziert das
      # Image über seinen Tag. Würde der Tag beim Deployment noch nicht existieren,
      # würde die neue Revision beim Start fehlschlagen (Image-Pull-Error).
      - name: In GitHub Container Registry einloggen
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Backend-Image bauen und pushen
        run: |
          IMAGE_TAG="${IMAGE_NAME,,}:${{ github.sha }}"
          echo "IMAGE_TAG=$IMAGE_TAG" >> "$GITHUB_ENV"
          docker build -f apps/api/Dockerfile -t "$IMAGE_TAG" .
          docker push "$IMAGE_TAG"

      - name: RAG-Image bauen und pushen
        run: |
          RAG_IMAGE_TAG="${RAG_IMAGE_NAME,,}:${{ github.sha }}"
          echo "RAG_IMAGE_TAG=$RAG_IMAGE_TAG" >> "$GITHUB_ENV"
          docker build -f services/rag/Dockerfile -t "$RAG_IMAGE_TAG" .
          docker push "$RAG_IMAGE_TAG"

      # --- 2. Bei Azure einloggen (OIDC / Federated Credentials) ---------------------
      - name: Bei Azure einloggen
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Resource Group sicherstellen
        run: |
          az group create \
            --name "$AZURE_RESOURCE_GROUP" \
            --location "$AZURE_LOCATION" \
            --output none

      # --- 3. Infrastruktur deployen (Bicep) ------------------------------------------
      # GHCR_PAT (nicht das ephemere GITHUB_TOKEN!) wird als Registry-Passwort in die
      # Container App geschrieben, weil sie das Image ggf. Stunden/Tage später aus dem
      # Stillstand heraus erneut zieht (Scale-to-Zero) - da ist GITHUB_TOKEN längst
      # abgelaufen.
      - name: Infrastruktur deployen
        id: infra
        # Secrets kommen über env: rein statt direkt per ${{ }} in den Bash-Befehl
        # interpoliert zu werden. Grund: ${{ secrets.X }} wird VOR der Bash-Ausführung
        # als reiner Text eingesetzt - stünde der Wert direkt in doppelten
        # Anführungszeichen im Skript, würde Bash "$"-Zeichen darin als (leere)
        # Variablen interpretieren. bcrypt-Hashes fangen immer mit "$2b$" an, das hat
        # AUTH_PASSWORD_HASH bisher bei jedem Deploy unbemerkt zerstört. Der Umweg über
        # env: + "$VARNAME" liest den Wert dagegen unverändert aus der Umgebungsvariable,
        # ohne ihn nochmal auf "$"-Sequenzen zu parsen.
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GHCR_PAT: ${{ secrets.GHCR_PAT }}
          AUTH_USERNAME: ${{ secrets.AUTH_USERNAME }}
          AUTH_PASSWORD_HASH: ${{ secrets.AUTH_PASSWORD_HASH }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: |
          OUTPUTS=$(az deployment group create \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.json \
            --parameters \
              databaseUrl="$DATABASE_URL" \
              anthropicApiKey="$ANTHROPIC_API_KEY" \
              groqApiKey="$GROQ_API_KEY" \
              containerImage="$IMAGE_TAG" \
              ragContainerImage="$RAG_IMAGE_TAG" \
              registryUsername="${{ github.actor }}" \
              registryPassword="$GHCR_PAT" \
              authUsername="$AUTH_USERNAME" \
              authPasswordHash="$AUTH_PASSWORD_HASH" \
              jwtSecret="$JWT_SECRET" \
            --query properties.outputs -o json)

          echo "container_app_url=$(echo "$OUTPUTS" | jq -r '.containerAppUrl.value')" >> "$GITHUB_OUTPUT"
          echo "static_web_app_name=$(echo "$OUTPUTS" | jq -r '.staticWebAppName.value')" >> "$GITHUB_OUTPUT"

      # --- 3b. Datenbank-Migrationen anwenden -------------------------------------------
      # Neon ist standardmäßig öffentlich erreichbar (kein IP-Firewall-Handshake wie bei
      # Azure Postgres Flexible Server nötig), daher entfällt das Öffnen/Schließen einer
      # Firewall-Regel für den Runner.
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Migrationen anwenden
        working-directory: apps/api
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx prisma migrate deploy

      # --- 4. Frontend bauen ------------------------------------------------------------
      # NEXT_PUBLIC_API_URL muss zur BUILD-Zeit gesetzt sein (landet im statischen JS-
      # Bundle) - deshalb erst jetzt, nachdem wir die frisch deployte Container-App-URL
      # aus Schritt 3 kennen. Node/npm ci laufen schon oben für die Migrationen.
      # Der Deploy-Schritt meldet Erfolg, sobald Azure das neue Image angenommen hat,
      # nicht erst, wenn die App damit auch startet. job-application-skill#14: Die API
      # startete nicht, der Deploy war trotzdem grün. Deshalb hier warten, bis genau
      # die neue Revision in Azure als gesund läuft UND der Endpunkt antwortet.
      # Nur der Endpunkt allein reicht nicht: Er hat trotz kaputter Revision 200 geliefert.
      - name: Smoke-Test der neuen Revision
        env:
          APP: trip-planner-dev-api
          RG: ${{ env.AZURE_RESOURCE_GROUP }}
          URL: ${{ steps.infra.outputs.container_app_url }}/health
          MAX_CODE: "300"
        run: |
          REV=$(az containerapp show -n "$APP" -g "$RG" --query properties.latestRevisionName -o tsv)
          echo "Neue Revision: $REV"
          for i in $(seq 1 30); do
            CODE=$(curl -s -o /dev/null -m 15 -w '%{http_code}' "$URL" || true)
            STATE=$(az containerapp revision show -n "$APP" -g "$RG" --revision "$REV" \
              --query "join('/', [properties.healthState, properties.runningState])" -o tsv)
            echo "Versuch $i: HTTP $CODE, Revision $STATE"
            case "$STATE" in
              *Failed*) echo "::error::Revision $REV startet nicht ($STATE)"; exit 1 ;;
              */Stopped)
                LATEST=$(az containerapp show -n "$APP" -g "$RG" --query properties.latestRevisionName -o tsv)
                if [ "$LATEST" != "$REV" ]; then echo "Revision wurde durch $LATEST abgelöst"; exit 0; fi ;;
              Healthy/Running*)
                if [ "$CODE" -ge 200 ] && [ "$CODE" -lt "$MAX_CODE" ]; then echo "OK"; exit 0; fi ;;
            esac
            sleep 10
          done
          echo "::error::Revision $REV nach 5 Minuten nicht gesund"; exit 1

      - name: Frontend bauen
        working-directory: apps/web
        env:
          NEXT_PUBLIC_API_URL: ${{ steps.infra.outputs.container_app_url }}
        run: npm run build

      # --- 5. Frontend zur Static Web App deployen --------------------------------------
      - name: Deployment-Token abrufen
        id: swa-token
        run: |
          TOKEN=$(az staticwebapp secrets list \
            --name "${{ steps.infra.outputs.static_web_app_name }}" \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --query "properties.apiKey" -o tsv)
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Frontend deployen
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ steps.swa-token.outputs.token }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: apps/web/out
          skip_app_build: true
